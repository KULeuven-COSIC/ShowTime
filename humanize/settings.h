
/////////////////////////////////
// Experimental parameters
#define ITERATIONS 20                   // number of measurements, including calibration
#define AMP_TRAVERSE 3000000            // amplifier length, was 3M for the RuhrSec live demo
#define SILENT_MODE 1                   // don't report helper stuff
#define NO_HELPER 0                     // disable helper thread, which somehow helps for I/O

#define PRIME_ITERATIONS 5              // Knob for cleaning LLC set across runs

#define RANDOM_TARGET 1                 // Randomly select the target address (and hence the LLC set)
/////////////////////////////////
